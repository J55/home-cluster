---
# NUC0 uses igc driver; match by MAC prefix to alias to net0 for bond0
apiVersion: v1alpha1
kind: LinkAliasConfig
name: net0
selector:
  match: link.driver == "igc" && mac(link.permanent_addr).startsWith("88:ae:dd:72:")
